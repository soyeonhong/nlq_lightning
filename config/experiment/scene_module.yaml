# @package _global_

# usage: python run.py +experiment=scene_module

model:
  scene_module: True
  scene_arch: 'encoder'

dataset:
  scene_module: True
  scene_module_path: '/data/soyeonhong/nlq/nlq_lightning/data/scene'
  max_scene: 15
  in_scene: 'mean'