# @package _global_

# usage: python run.py +experiment=object_mask

dataset:
  use_egovlp: True
  env_dir: '/data/soyeonhong/nlq/nlq_lightning/universal-ner/LLaVA-NeXT-Video-7B-DPO-uniner-extracted/version-1'
  env_interval: 2
  max_obj_in_cap: 15 # this is hard coding
  max_cap_len: 240 # this is hard coding

model:
  use_egovlp: True
  egovlp_path: '/data/soyeonhong/nlq/nlq_lightning/data/egovlp-config-removed.pth'
  max_obj_in_cap: ${dataset.max_obj_in_cap}
  max_cap_len: ${dataset.max_cap_len}
  use_matmul: False
  use_sim: False
  topk: 5